/* os/lock.h — FreeBSD shim. Public Apple lock header. The internals
 * we need are in os/lock_private.h; this just re-exposes them. */
#ifndef _FREEBSD_SHIM_OS_LOCK_H_
#define _FREEBSD_SHIM_OS_LOCK_H_

#include <os/lock_private.h>

#endif
