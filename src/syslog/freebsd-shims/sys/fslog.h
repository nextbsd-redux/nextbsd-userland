/* sys/fslog.h — FreeBSD shim. Apple's filesystem event log header.
 * syslogd transitively pulls it in via some asl_action paths.
 * No actual symbols used — empty stub. */
#ifndef _FREEBSD_SHIM_SYS_FSLOG_H_
#define _FREEBSD_SHIM_SYS_FSLOG_H_
#endif
