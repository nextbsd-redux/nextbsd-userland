/* mach/mach_init.h — FreeBSD shim. Apple's macro/struct for Mach
 * task initialization (mach_task_self_, etc.). Our libmach covers
 * the symbols; this header just re-exposes them via mach/mach.h. */
#ifndef _FREEBSD_SHIM_MACH_MACH_INIT_H_
#define _FREEBSD_SHIM_MACH_MACH_INIT_H_

#include <mach/mach.h>

#endif
